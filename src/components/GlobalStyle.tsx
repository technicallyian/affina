"use client";

import React from 'react';

const GlobalStyle = () => (
  <style jsx global>{`
    body {
      /* cursor: none; */ /* Removed to keep the default cursor visible */
    }
  `}</style>
);

export default GlobalStyle; 